.claude/
_bmad/
_bmad-output/
design-system/
product-brief.md/
/
# Node
node_modules/

# Astro build output and type cache
dist/
.astro/

# Wrangler local state (D1/R2/KV emulation) — never commit
.wrangler/

# Local dev secrets — never commit (use .dev.vars.example as template)
.dev.vars

# OS
.DS_Store
Thumbs.db

# Local agent / temp tooling
.omo/
.opencode/

# Office temp lock files
~$*
**/~$*

# Source data / village docs — keep local only, never push to GitHub
*.xlsx
*.xls
*.docx
*.doc
*.ppt
*.pptx
*.pdf
!public/modul-panduan.pdf
!public/templates/**/*.docx
!public/images/kop-desa-citarik.png

# Large / offline-only notes (if present at repo root)
DATA UMKM*
DAFTAR USAHA*
PROFIL DESA*
Profil Desa*
REKAPITULASI*
SURAT KETERANGAN*


